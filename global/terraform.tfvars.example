# Copy to terraform.tfvars (gitignored) and fill in.
project = "cloudops"

# Monthly spend ceiling in USD. Alerts fire at 80% actual and 100% forecast.
monthly_budget_usd = 20

# REQUIRED - has no default. Cost alerts are delivered here.
alert_emails = ["you@example.com"]

# GitHub Actions OIDC federation.
github_owner = "sahrudindev"
github_repo  = "terraform-aws-platform"

# Same bucket you put in backend.hcl.
state_bucket = "cloudops-tfstate-<AWS_ACCOUNT_ID>"

# From `terraform output state_kms_key_arn` in bootstrap/.
state_kms_key_arn = "arn:aws:kms:ap-southeast-1:<AWS_ACCOUNT_ID>:key/<KEY_ID>"

# Only needed when the account issues immutable OIDC subject claims. Check with:
#   gh api repos/<owner>/<repo>/actions/oidc/customization/sub
github_owner_id = 12345678
github_repo_id  = 987654321
